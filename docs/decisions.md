# Decisions log

Ambiguities resolved autonomously per CLAUDE.md / GOAL_PROMPT.md. One line each: date, decision, reason.

- 2026-07-17: Use Node 24 locally with `engines: ">=22"` and Node 22 in CI — the `ai` v7 package requires Node >= 22; nvm had 24 installed.
- 2026-07-17: Pin TypeScript 5.9 (create-next-app default) instead of the brand-new TS 7 — ecosystem compatibility over novelty; strict mode is what the spec requires.
- 2026-07-17: `createDb()` auto-applies migrations on open; seeding stays an explicit command — a fresh clone works right after install without hidden data writes.
- 2026-07-17: Session 0 runs the same six-step lesson flow as regular sessions, with step content mapped onto the canon's method/exercise fields — one state machine, isomorphic flow per GOAL §5.3.
- 2026-07-17: Theses get deterministic ids `s{n}-t{index+1}` generated at parse/seed time — curriculum.json carries none, and the canon-only guardrail needs id-addressable theses.
- 2026-07-17: All 95 canon-backed review cards (31 repères + 64 theses) are created due-immediately at seed time — the spec fixes the card counts; activation-on-lesson would add state for no tested benefit.
- 2026-07-17: Review rounds are capped at 10 cards (`REVIEW_ROUND_LIMIT`) — makes "one round of the queue" a bounded, testable unit; FSRS ordering decides which ten.
- 2026-07-17: The reveal button shows canon material only (core/method/reperesNote/theses of the session) — no generated model answer can leak past the guardrail.
- 2026-07-17: The mock grader keys on answer length (>= 40 chars strong, < 40 lapse-worthy; cards >= 10 chars pass) — deterministic fixtures that let tests exercise both the strong and lapse paths.
- 2026-07-17: Default model slots seeded as tutor/grader=claude-opus-4-8, light=claude-haiku-4-5 — current Anthropic guidance (most capable Opus tier by default, fast tier for light roles); changeable in settings.
- 2026-07-17: E2E resets its SQLite inside the Playwright webServer command (not globalSetup) — the dev server must never hold a connection to a database file deleted from under it.
- 2026-07-17: Streamed chat returns plain text; the client refetches lesson state after each exchange — the server stays the single authority on step transitions with no custom wire protocol.
- 2026-07-17: Repository lives at the repo root created with the canon commit; the working-directory parent (which held stray copies of CLAUDE.md/GOAL_PROMPT.md) is not part of the project.
- 2026-07-17: OAuth support added alongside API keys — credentials resolve as ANTHROPIC_API_KEY → ANTHROPIC_AUTH_TOKEN → `ant auth login` profile (short-lived tokens minted via `ant auth print-credentials`, cached 5 min); OAuth requests carry the `oauth-2025-04-20` beta header, and exactly one auth mechanism is ever sent.
- 2026-07-17: Third provider `claude-code` (Claude Agent SDK) added so the app can run on a Claude Pro/Max subscription for local personal use — dialogue history is serialized into one prompt per query, advance_step is an SDK MCP tool, structured output uses the CLI's json_schema mode (draft-7, `$schema` stripped — the CLI validator rejects the 2020-12 marker). Selected explicitly via LLM_PROVIDER=claude-code, never auto-detected.
- 2026-07-17: Learner feedback rework — /lessons/[n] is now a textbook reading page (all canon sections + AI model answers per canonical question, generated once and cached; role `modelAnswer` on the grader slot); the dialogue moved to /lessons/[n]/dialogue with a learner-initiated advance button (same server-side production gate as the tutor tool), visible step goals, and a 2–3-exchanges-per-step pacing instruction; the roadmap shows in-progress runs (対話中 n/6).
- 2026-07-17: The reveal guardrail (event + forced variant) stays scoped to *practice*; reading model answers on the textbook page is intentionally friction-free — the learner explicitly asked for a textbook to study from, and the textbook nudges self-attempt in copy instead (ADR 0007 amended).
- 2026-07-17: Body typography switched from Noto Serif JP to the system sans-serif stack (Helvetica / Hiragino Sans / Meiryo) at the learner's request; the serif italic remains only in the Émile logotype. Also removes the Google Fonts build-time dependency. CLAUDE.md style rule updated to match.
- 2026-07-17: The repères constellation on the roadmap was replaced by plain labeled progress bars (lessons completed / repères reviewed / theses reviewed) — learner feedback: the dots were illegible and unexplained; clarity beats decoration.
- 2026-07-17: Added the generated reading layer (読み物) — per-session friendly study text for Japanese teens, structured by the 5E model with ARCS motivation and Mayer's personalization principle (conversational 'you' voice; see docs/writing-guide.md for sources); generated once from the canon on the tutor slot, cached in session_readings, thesesGuide ids canon-checked on write; the raw canon stays available in a reference accordion. Analogies may name works/people but never quote them.
- 2026-07-17: Added the global Q&A chat (質問, /ask) — a top-nav advisor role that answers with a compact digest of the entire canon (all sessions/reperes/theses with ids) in cached system context, always cites the sessions worth reading (第N回 mentions are linkified to textbook pages), keeps one persistent clearable thread, and declines to ghost-write essays (nudges toward graded practice instead).
- 2026-07-18: Presumption-free personalization rule added to the reading constitution after learner feedback ("the premise about the reader is wrong" — the s0 hook asserted the reader would feel stuck without work, foreclosing positions like "I'd spend all my time on my own creative work"): never assert the reader's inner reaction (「詰まりましたよね」「〜ですよね」「〜たはずです」禁止), present plural intuitions as equally legitimate entry points, and explicitly welcome third positions as material. Five cached readings (s0/s2/s3/s6/s9) matched presumptive patterns and were regenerated.
